# JUnit with Groovy example

This example demonstrates how to write unit tests for your Jenkins pipeline shared library using Groovy instead of Java.

## Purpose

Since Jenkins pipelines and shared libraries are written in Groovy, it is often natural and convenient to write tests in Groovy as well.
This example configures the project to run Groovy-based unit tests using JUnit and `JenkinsPipelineUnit`.
